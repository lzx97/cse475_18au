#include "helper.h"

switch(pid) {
        case PID_SET_GLOBALS:
            
        case PID_STOP:

        case PID_START:

        case PID_PLAY_SOUND:

        case PID_PLAY_EFFECT:

        case PID_BROADCAST_STATES:

        case PID_STARTLE:

        case PID_SEND_STATE:

        default: 
            return nullptr;
    }